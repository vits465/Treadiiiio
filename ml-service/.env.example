# ML Service FastAPI settings
HOST=127.0.0.1
PORT=8000

# Path to the shared SQLite database
DB_PATH=../forex_bot.db

# Must match the API_SECRET_KEY in the main .env
API_SECRET_KEY=change_me_to_a_random_32_char_hex
